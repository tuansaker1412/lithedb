app-id: org.tableprolinux.App
runtime: org.gnome.Platform
runtime-version: '45'
sdk: org.gnome.Sdk//45
command: table-pro-linux

finish-args:
  - --share=network
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  - --talk-name=org.freedesktop.secrets

modules:
  - name: table-pro-linux
    buildsystem: simple
    build-commands:
      - cargo --offline fetch --manifest-path Cargo.toml
      - cargo build --release --manifest-path Cargo.toml
      - install -Dm755 target/release/table-pro-linux /app/bin/table-pro-linux
      - install -Dm644 packaging/flatpak/org.tableprolinux.App.desktop /app/share/applications/org.tableprolinux.App.desktop
      - install -Dm644 packaging/flatpak/org.tableprolinux.App.metainfo.xml /app/share/metainfo/org.tableprolinux.App.metainfo.xml
      - install -Dm644 packaging/flatpak/org.tableprolinux.App.svg /app/share/icons/hicolor/scalable/apps/org.tableprolinux.App.svg
    sources:
      - type: dir
        path: ../..
      - type: file
        path: cargo-sources.json
